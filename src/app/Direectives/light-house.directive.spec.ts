import { LightHouseDirective } from './light-house.directive';

describe('LightHouseDirective', () => {
  it('should create an instance', () => {
    const directive = new LightHouseDirective();
    expect(directive).toBeTruthy();
  });
});
